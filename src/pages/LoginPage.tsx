import { Typography } from "@mui/material";

export default function LoginPage() {
  return (
    <Typography variant="h5" sx={{ mt: "10%" }}>
      Для дальнейших действий необходима авторизация
    </Typography>
  );
}
